from django.conf.urls import url
from . import views

# urlpatterns = [
#     url(r'^selectitems/', views.select_items, name="select items"),
#     url(r'^$', views.landing_page, name="landing page")
# ]
